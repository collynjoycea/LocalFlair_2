 :root {
            --bg-color: #fdf2e9; /* Light peach background from image */
            --sidebar-width: 250px;
            --primary-text: #3b1f0f;
            --muted-text: #6f5b44;
            --card-shadow: 0 4px 15px rgba(0,0,0,0.05);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Segoe UI', Roboto, sans-serif; }

        body { background: var(--bg-color); color: var(--primary-text); }

        .container { display: flex; min-height: 100vh; }

        /* MAIN CONTENT AREA FIX */
        .main {
            flex: 1;
            margin-left: var(--sidebar-width);
            padding: 25px;
            padding-top: 90px; /* Space for fixed topbar */
        }

        /* TOPBAR STYLING */
        .topbar {
            position: fixed;
            top: 15px;
            right: 25px;
            left: calc(var(--sidebar-width) + 25px);
            height: 60px;
            background: rgba(255, 255, 255, 0.5);
            backdrop-filter: blur(10px);
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 25px;
            border-radius: 15px;
            box-shadow: var(--card-shadow);
            z-index: 1000;
        }

        .search-box {
            background: #fff;
            padding: 8px 15px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            width: 350px;
        }

        .search-box input {
            border: none;
            outline: none;
            margin-left: 10px;
            width: 100%;
            font-size: 14px;
        }

        .user-profile { display: flex; align-items: center; gap: 15px; }
        .user-profile img { width: 35px; height: 35px; border-radius: 50%; object-fit: cover; }












        <?php
session_start();
// Temporary admin check placeholder
if (!isset($_SESSION['admin_id'])) {
    // header("Location: login.php");
    // exit();
}
?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LocalFlair | Dashboard</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <style>
        :root {
            --bg-color: #fdf2e9; /* Light peach background from image */
            --sidebar-width: 250px;
            --primary-text: #3b1f0f;
            --muted-text: #6f5b44;
            --card-shadow: 0 4px 15px rgba(0,0,0,0.05);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Segoe UI', Roboto, sans-serif; }

        body { background: var(--bg-color); color: var(--primary-text); }

        .container { display: flex; min-height: 100vh; }

        /* MAIN CONTENT AREA FIX */
        .main {
            flex: 1;
            margin-left: var(--sidebar-width);
            padding: 25px;
            padding-top: 90px; /* Space for fixed topbar */
        }

        /* TOPBAR STYLING */
        .topbar {
            position: fixed;
            top: 15px;
            right: 25px;
            left: calc(var(--sidebar-width) + 25px);
            height: 60px;
            background: rgba(255, 255, 255, 0.5);
            backdrop-filter: blur(10px);
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 25px;
            border-radius: 15px;
            box-shadow: var(--card-shadow);
            z-index: 1000;
        }

        .search-box {
            background: #fff;
            padding: 8px 15px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            width: 350px;
        }

        .search-box input {
            border: none;
            outline: none;
            margin-left: 10px;
            width: 100%;
            font-size: 14px;
        }

        .user-profile { display: flex; align-items: center; gap: 15px; }
        .user-profile img { width: 35px; height: 35px; border-radius: 50%; object-fit: cover; }

        /* COMPACT CARDS */
        .cards {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 20px;
            margin-bottom: 25px;
        }

        .card {
            padding: 20px;
            border-radius: 20px;
            box-shadow: var(--card-shadow);
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            height: 130px; /* Slimmer height */
        }

        .card h4 { font-size: 13px; color: var(--muted-text); font-weight: 600; }
        .card p { font-size: 24px; font-weight: bold; margin-top: 5px; }
        .card i { font-size: 20px; align-self: flex-start; margin-bottom: 10px; }

        /* Card Colors */
        .card.users { background: #fde6d8; }
        .card.orders { background: #e6f0e8; }
        .card.sales { background: #fff0d9; }
        .card.pending { background: #fde2e2; }

        /* CHARTS SECTION */
        .charts-row {
            display: grid;
            grid-template-columns: 1.5fr 1fr;
            gap: 20px;
            margin-bottom: 25px;
        }

        .chart-box {
            background: #fff;
            padding: 20px;
            border-radius: 20px;
            box-shadow: var(--card-shadow);
        }

        .chart-box h3 { font-size: 16px; margin-bottom: 15px; }
        .chart-container { height: 220px; position: relative; }

        /* TABLES */
        .tables-row {
            display: grid;
            grid-template-columns: 1.5fr 1fr;
            gap: 20px;
        }

        .table-box {
            background: #fff;
            padding: 20px;
            border-radius: 20px;
            box-shadow: var(--card-shadow);
        }

        table { width: 100%; border-collapse: collapse; margin-top: 10px; }
        th { text-align: left; padding: 12px; background: #fdf2e9; font-size: 13px; color: var(--muted-text); }
        td { pad